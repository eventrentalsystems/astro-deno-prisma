{
  "name": "tasty-trappist",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "astro": "astro",
    "preview": "astro build && deno run --allow-net --allow-read --allow-env ./dist/server/entry.mjs",
    "pkg": "astro build && deno compile --allow-net --allow-read --allow-env ./dist/server/entry.mjs -o server"
  },
  "dependencies": {
    "@astrojs/deno": "^4.2.0",
    "astro": "^2.8.0"
  }
}
